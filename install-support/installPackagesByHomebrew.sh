has autoconf || brew install autoconf
has aws || brew install awscli
brew install bash
has cask || brew install cask
brew install emacs
has flyway || brew install flyway
has git || brew install git
has gsed || brew install gnu-sed
brew install libevent
brew install libssh2
has mysql || brew install mysql
brew install openssl
has peco || brew install peco
has pkg-config || brew install pkg-config
has pyenv || brew install pyenv
brew install readline
brew install reattach-to-user-namespace
has rust || brew install rust
has sbt || brew install sbt
has tmux || brew install tmux
has wget || brew install wget
