(function(global) {
  "use strict";

  // --- dependency modules ----------------------------------
  //var Parent = require("./parent")

  // --- define / local variables ----------------------------
  // --- class / interfaces ----------------------------------
  //function MyClass() {}

  //MyClass["prototype"]["myMethod"] = myMethod; // myMethod(param:String):Boolean

  // --- implements ------------------------------------------
  //function myMethod(param) {
  //  ...
  //}

  // --- exports ---------------------------------------------
  //if (typeof module !== "undefined") {
  //  module["exports"] = MyClass;
  //}
  //global["MyClass"] = MyClass;

})((this || 0).self || global);
